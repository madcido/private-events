<%= form_for @user, url: login_path, html: {class: "col flex-end"} do |f| %>

    <div class="input">
        <%= f.label :name %>
        <%= f.text_field :name %>
    </div>

    <%= f.submit "Sign Up/Log In", class: "submit-btn"%>

<% end %>