<nav class="navbar row space-around">
    <%= link_to "All Events", root_path %>
    <% if logged_in? %>
        <p>Welcome, <%= link_to current_user.name, user_path(current_user.id) %></p>
    <% end %>
    <div class="row">
        <% if logged_in? %>
            <%= link_to "Log Out", logout_path, method: :delete %>
        <% else %>
            <%= link_to "Log In", login_path %>
        <% end %>
    </div>
</nav>